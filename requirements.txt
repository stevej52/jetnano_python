# Core (both ends)
pyserial>=3.4
# Operator side (Pi or PC)
pygame>=2.0
# Robot side (Jetson Nano); needs Adafruit Blinka for the I2C bus
adafruit-circuitpython-servokit>=1.3
adafruit-circuitpython-bno055
# Optional links and sensors
pyModbusTCP>=0.2
# pyrealsense2   # only if the RealSense obstacle guard is enabled
# Tests
pytest>=7
